"
The text the browser is filtered by has changed, so the lists show
something else. See {{gtClass:name=CarpBrowserModel}}.
"
Class {
	#name : #CarpBrowserFilterChanged,
	#superclass : #Announcement,
	#category : #'Carp-Browser'
}
