"
Announced by a {{gtClass:name=CarpMacroExpansion}} whenever stepping moves
it, so views over it refresh instead of showing where the stepper used to
be.
"
Class {
	#name : #CarpMacroExpansionChanged,
	#superclass : #Announcement,
	#category : #'Carp-Tools'
}
