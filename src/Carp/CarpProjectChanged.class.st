"
Announced by a {{gtClass:name=CarpProject}} when its tree is re-read or its
compiler session changes state, so views over it refresh.
"
Class {
	#name : #CarpProjectChanged,
	#superclass : #Announcement,
	#category : #'Carp-Project'
}
